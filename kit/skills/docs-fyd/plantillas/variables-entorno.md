> Documento derivado automáticamente del código de este repositorio — se regenera, no editar a mano en silencio; lo que el código no sabe va en `_ACLARACIONES.md`.
> 🔒 Solo el NOMBRE de cada variable, nunca el valor (todo a la derecha del `=` se corta).

# Variables de entorno y servicios externos — [Nombre de la aplicación]

## 1. Variables de entorno

Todas las que usa la aplicación — **solo el nombre y para qué sirve, sin valores**.

| Variable | Dónde se usa (archivo) | Para qué sirve |
|---|---|---|
| [NOMBRE] | [archivo] | [propósito] |

## 2. Servicios / APIs externos

Con qué servicios o APIs se conecta y en qué parte del código se los llama.

| Servicio / Proveedor | Dónde se llama (archivo) | Para qué |
|---|---|---|
| [Supabase / OpenAI / Anthropic / Stripe / Resend / ...] | [archivo] | [uso] |
